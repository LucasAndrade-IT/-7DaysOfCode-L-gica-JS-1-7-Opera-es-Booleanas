let numeroUm = 1
let stringUm = '1'
let numeroTrinta = 30
let stringTrinta = '30'
let numeroDez = 10
let stringDez = '10'

if (numeroUm == stringUm && numeroUm === stringUm) {
  console.log(`As variáveis ${numeroUm} e ${stringUm} tem o mesmo valor.`)
} else {
  console.log(`As variáveis ${numeroUm} e ${stringUm} tem o mesmo valor, mas tipos diferentes.`)
}

if (numeroTrinta == stringTrinta && numeroTrinta === stringTrinta) {
console.log(`As variáveis ${numeroTrinta} e ${stringTrinta} tem o mesmo valor.`)
} else {
console.log(`As variáveis ${numeroTrinta} e ${stringTrinta} tem o mesmo valor, mas tipos diferentes.`)
}

if (numeroDez == stringDez && numeroDez === stringDez) {
  console.log(`As variáveis ${numeroDez} e ${stringDez} tem o mesmo valor.`)
} else {
  console.log(`As variáveis ${numeroDez} e ${stringDez} tem o mesmo valor, mas tipos diferentes.`)
}
